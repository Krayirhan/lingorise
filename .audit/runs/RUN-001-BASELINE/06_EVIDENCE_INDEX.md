# 06 — Evidence Index

| Evidence ID | Level | Check/source | Result | Confidence | Limitations |
|---|---|---|---|---|---|
| E-CORE-01 | E2_TEST | `npm test` (`tests/testSuite.ts`), executed this run | 283/283 assertions pass, 0 failed. Covers SRS intervals/jitter/leech/taper, mastery, badges/quests, XP-application path, distractor/reverse-mode logic. | HIGH | Custom assertion runner, not a coverage-instrumented framework — no line/branch % available. |
| E-CORE-02 | E3_STATIC | `src/domain/gamification/streak.ts:32-48`, direct read | `updateDailyStreak` only special-cases `diffDays === 1`; any other diff (including 0/negative from a clock moving backward) resets streak to 1. No test in `testSuite.ts` exercises a backward-clock scenario. | HIGH | Source-verified logic bug; real-world frequency of backward-clock events not independently measured (inference on likelihood, not on the code behavior itself). |
| E-CORE-03 | E3_STATIC + E2_TEST(absence) | `src/content/questions/difficulty.ts` vs `grep -n "computeXpReward\|computeDifficulty" tests/testSuite.ts` | Zero references in the test suite; functions are called only at content-module load time to bake `xp`/`difficulty` onto ~590 static question objects. | HIGH | Confirms absence of direct tests; does not prove the current formula is wrong, only that a regression would be silent. |
| E-DATA-01 | E3_STATIC | `src/services/storage.ts` (`loadUserData`/`saveUserData`), direct read | `loadUserData` has try/catch with fallback to `DEFAULT_USER_DATA` + `console.warn` — safe. `saveUserData` has try/catch but only warns on failure — a persistent write failure (e.g. disk full) is invisible to the user; app continues as if saved. | HIGH | Source-verified; not reproduced by actually filling device storage in this run. |
| E-DATA-02 | E3_STATIC + E6_INFERENCE | `src/app/AppBootstrap.tsx:26-40` vs `src/state/useUserProgress.ts:47-81`, direct read of both effects | Both independently call `loadUserData()`/mutate/`saveUserData()` on cold start with no shared sequencing; a plausible interleaving lets `useUserProgress.init()`'s plain (non-merged) overwrite land after `AppBootstrap`'s merged write, silently discarding remote-only `learningProgress` entries until the next sign-in cycle. | MEDIUM | Race timing was reasoned from source, not reproduced with an artificial network-delay test in this run — labeled as inference on the interleaving, not as an observed failure. |
| E-DATA-03 | E3_STATIC | `firestore.rules:6-43`, direct read | Per-user subcollections (`progress`, `dailyTasks`, `items` under `users/{userId}`) correctly require `request.auth.uid == userId`; public catalogue collections are `read: true, write: false`. No overly permissive rule found. | HIGH | Rules read directly, not exercised via a live emulator run this session (see `05_TOOL_AND_GRAPH_ANALYSIS.md`); cross-checked against `tests/firestoreRules.test.ts`'s asserted ALLOW/DENY scenarios by inspection, not execution. |
| E-DATA-04 | E3_STATIC | `src/services/firestore.ts:81-109` (`deleteUserData`) + `grep -rn "getDailyTaskCollection" src/` | `deleteUserData` purges `items` (batched) and `progress/main` and the root doc. `getDailyTaskCollection` (firestore.ts:235-236) has **zero call sites anywhere in `src/`** — no code ever writes to `users/{uid}/dailyTasks` in Firestore, so its absence from the deletion routine does not orphan any real data. Daily quests live only in local `UserData.dailyQuests`. | HIGH | Confirms the collection is genuinely unused, not merely under-audited; downgrades an initial sub-agent claim of a privacy gap to a P4 dead-code note (see `09_FINDINGS.md` correction log). |
| E-REL-01 | E3_STATIC | `src/components/ErrorBoundary.tsx` + `src/app/AppBootstrap.tsx:83-92`, direct read | A top-level `ErrorBoundary` wraps the main `AppNavigator` render; a render-time exception during practice is caught, not fatal. Does not wrap the `AuthScreen`/loading branches (lines 61-80). | HIGH | Source-verified for wrapping structure; not reproduced with an intentionally-thrown render error in this run. |
| E-REL-02 | E3_STATIC | `src/state/useUserProgress.ts:113-138` (`refresh`, current revision, post SRS-hardening fix) | `refresh()`'s `Promise.all([syncUserData, syncUserProgress])` at the end of the function has no surrounding try/catch, unlike every other Firestore call site in the same file. A network failure during a manual pull-to-refresh can throw uncaught to the caller. | MEDIUM | Confirmed by direct read; actual caller-side handling (whether `AppNavigator`'s `onRefresh` swallows it) not traced in this pass. |
| E-ARCH-01 | E3_STATIC | `grep -rn "services/spacedRepetition" src/` | Zero importers of `src/services/spacedRepetition.ts` anywhere in `src/`. | HIGH | Confirms dead code, not merely unused-by-convention. |
| E-ARCH-02 | E3_STATIC | `diff src/utils/formatters.ts src/i18n/formatters.ts` + `grep` for all 4 real importer files | Byte-near-identical content; all real call sites import `utils/formatters.ts` only — `i18n/formatters.ts` has zero importers. | HIGH | Corrects an initial sub-agent claim that both copies were "actively used" — verified independently before inclusion. |
| E-ARCH-03 | E3_STATIC | `wc -l src/screens/AuthScreen.tsx src/features/profile/components/AccountManagementCard.tsx src/state/useUserProgress.ts` | 662 / 594 / 409 lines respectively, each combining multiple distinct responsibilities in one file (confirmed by direct read of `AuthScreen.tsx`'s login/register/reset/guest sections). | HIGH | Line count and responsibility mix independently confirmed; "should this be split" is a judgment call disclosed as such, not asserted as an objective defect. |
| E-TEST-01 | E3_STATIC | `.github/workflows/ci.yml`, direct read | CI runs `typecheck` → `test` → `test:rules` on PR and push to `main`. Maestro E2E (`test:e2e:smoke`) is not included in CI. | HIGH | Source-verified. |
| E-TEST-02 | E3_STATIC | `package.json` devDependencies + `find src -iname "*.test.tsx" -o -iname "*.spec.tsx"` | No Jest/React-Testing-Library/Detox dependency; zero component-level test files exist. | HIGH | Confirms zero UI-component test coverage exists as a structural fact, not a quality judgment about whether it's needed at this risk level (see `07_DETAILED_AUDIT.md` for the proportionality reasoning). |
| E-SEC-01 | E3_STATIC | `src/services/firebase.ts`, `src/services/auth.ts`, `src/services/telemetry.ts`, targeted grep for `apiKey:`/`secret`/`token =`/`console.log` of user objects | No hardcoded secret literals in `src/`; Firebase config is read from `process.env.EXPO_PUBLIC_FIREBASE_*`; no PII logged; telemetry never leaves the device. | HIGH | No secret VALUES were displayed during this check, per Secret Zero Exposure policy. |
| E-SEC-02 | E3_STATIC (self-flagged procedural note) | `.env.example` (git-tracked, public in repo history) | Contains what appear to be the actual production Firebase Web client config values rather than placeholder values. Firebase Web API keys are not secret-by-design (access is enforced by `firestore.rules`, not key secrecy — see E-DATA-03), so this is a hygiene note, not a vulnerability. **Procedural note:** this file was read directly by the audit; because it is git-tracked and its content is therefore already public in repository history, no new exposure occurred, but the check technically matches the Secret Zero Exposure policy's "do not `cat .env.*`" pattern and is logged here for transparency. The real `.env` (untracked, gitignored) was never read — only its existence/gitignore status was checked. | HIGH (on the finding) / disclosed (on the process note) | See `09_FINDINGS.md` → `SEC-002` for the resulting low-severity recommendation. |
| E-DEPLOY-01 | E1_RUNTIME | `gradlew assembleRelease` + `adb install -r` + `adb shell am start` + `adb logcat`, executed this run | `BUILD SUCCESSFUL`; APK installed on Pixel_9_Pro emulator; app launched, a practice session was driven through 2 questions (one wrong answer), no app-process crash/exception in logcat (only an unrelated OS-level Bluetooth-stack crash and a benign HWUI format warning). | HIGH | Single manual smoke pass on one emulator/ABI (x86_64); not a substitute for the un-run Maestro suite or a matrix of real devices. |
| E-DEPS-01 | E3_STATIC | `package.json` dependencies | React 19.2.3 / React Native 0.85.3 / Expo ~56 / Firebase JS SDK ^12.18.0 — all current-generation major versions, no obviously abandoned packages. | MEDIUM | No `npm audit` was executed this run (see `05_TOOL_AND_GRAPH_ANALYSIS.md` → Skipped tools); freshness assessed by version-number recency only, not by a vulnerability database check. |

## Evidence limitations (global)
- No crash-reporting/production telemetry service is integrated, so no real-world incident data exists to corroborate or refute source-level reliability findings — all reliability conclusions are `E3_STATIC`/`E2_TEST`, never `E1_RUNTIME`-in-production.
- `npm audit`, `firebase emulators:exec` (rules test execution), and the Maestro E2E suite were not executed this run; the relevant domains (Dependency health, Security, Testing) are scored conservatively where this gap matters, per Evidence Policy §"Scoring under uncertainty."
- Accessibility evaluation relies significantly on the project's own prior documented audit (`docs/roadmap/09-accessibility.md`) rather than a full independent re-audit of every screen in this pass — spot-checked, not exhaustively re-verified.
- This audit covers the Android build path only (the only buildable native project in this checkout); iOS-specific runtime behavior is `UNKNOWN`.
