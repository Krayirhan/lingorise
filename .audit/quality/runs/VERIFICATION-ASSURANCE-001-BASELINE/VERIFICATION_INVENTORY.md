# VERIFICATION-ASSURANCE-001-BASELINE — Verification Inventory (Pass A, Blind)

Revision: `16b9aab1f776503ec52067d4f543af8ab6f2e9aa`

Compiled before reading any domain audit findings, per this audit's own anti-anchoring rule.

| Verification type | Location | What it actually proves | What it does NOT prove | Current revision evidence |
|---|---|---|---|---|
| TypeScript compiler (strict mode) | Whole `src/` | Type-shape consistency at compile time; catches many category errors | Runtime behavior, business-rule correctness, integration behavior with real Firebase/AsyncStorage | E2 — `npm run typecheck` passes per shared discovery |
| Unit/domain tests | `tests/testSuite.ts` (1782 lines, 342 assertions, custom runner, plain `ts-node`) | Pure domain-function correctness (exam pass/fail boundaries, promotion logic, migration pipeline, XP/answer evaluation) when called directly with hand-constructed inputs | Anything requiring real `AsyncStorage` or React rendering — the file's own comments confirm neither is available in this test environment; also does not exercise the real Firestore-calling service functions | E3 (executable) for pure functions; structurally incapable of exercising the storage/network integration layer |
| Firestore Rules tests | `tests/firestoreRules.test.ts` (38 lines, ~12 `assertFails`/`assertSucceeds` calls) | Real authorization behavior against a genuine Firestore emulator running the actual `firestore.rules` file — owner/cross-user/anonymous/public-catalogue access | Field-level validation (rules have none, so nothing to test there), the app's own service-layer code that calls Firestore | E3 — executes real rules against a real emulator; CI runs this on JDK21 and passes on HEAD (local JDK17 run fails — an environment gap, not a defect, already established in a separate audit) |
| Maestro E2E | `.maestro/smoke.yaml` (one flow) | The app boots, completes onboarding, starts as guest, and reaches the Practice Hub screen (headline text assertion) | Any actual practice-answer interaction, exam flow, authentication/login, profile/settings, data management, or error/failure path | E4 (real device/emulator runtime) for exactly this one shallow path; extensive in-file comments document this single flow being fragile/hard-won during authoring (Maestro text-matching quirks, silent no-op taps) |
| Android build verification | CI `android-build` job | A release APK builds successfully from current source | Runtime correctness of the built APK beyond what `e2e-smoke` separately checks | E3 — confirmed green on HEAD in prior session evidence |
| CI orchestration | `.github/workflows/ci.yml` (3 jobs: verify, android-build, e2e-smoke) | Typecheck+test+rules, build, and E2E smoke all run repeatably and are currently green on HEAD | That failures technically block anything — no branch protection exists on `main` (confirmed in a separate, already-completed audit), so CI is a visible signal, not an enforced gate | E3 (repeatable, green) for the jobs themselves; the enforcement gap is a distinct, lighter-weight observation here (heavier branch-protection scoring belongs to a later Release/Repo-domain audit) |
| Static analysis (Semgrep) | N/A this session | N/A | N/A | Connection failure (CONNECT_TIMEOUT) in this and prior sessions — not usable as evidence, not treated as "unconfigured" |
| Manual/runtime evidence | Various prior-session Maestro screenshot evidence packs (consumer-design audits) | Visual/UX correctness at specific points in time, on specific builds | Durable regression protection — these are one-time observations, not automated tests that re-run on every change | E4 (real, but non-repeating) |

## Summary

The verification system has a genuine strength (pure domain-function unit testing, and real Firestore-rules-emulator authorization testing) and a genuine, consistent gap (almost nothing exercises the actual integration boundary — real AsyncStorage reads/writes, real Firestore service-layer calls, real UI interaction beyond one shallow E2E path). This shape — strong at the "pure logic" layer, weak at the "does it actually work end-to-end" layer — recurs throughout the detailed findings below.
