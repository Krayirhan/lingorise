# 06 — Evidence Index (REAUDIT)

New/updated evidence since `RUN-001-BASELINE/06_EVIDENCE_INDEX.md` (that index's entries remain valid for anything not re-touched this session).

| Evidence ID | Level | Check/source | Result | Confidence | Limitations |
|---|---|---|---|---|---|
| E2-CORE-01 | E2_TEST | `npm test`, fresh execution this run | **300/300 pass**, 0 failed (283 baseline + 17 new: CORE-001 backward-clock ×2, CORE-002 XP/difficulty ×9, CORE-003 quest-archive/bringForward ×6) | HIGH | Same custom-runner limitation as baseline (no line/branch % instrumentation). |
| E2-CORE-02 | E3_STATIC | `src/domain/gamification/streak.ts:44-49`, direct read | New `diffDays <= 0` branch added, returns unchanged streak + `isNewDay: false` instead of falling into the reset branch. Matches the fix acceptance criteria from `CORE-001`. | HIGH | — |
| E2-DATA-01 | E3_STATIC | `src/state/useUserProgress.ts:87` (`init`), direct read | `saveUserData(updated)` now conditional on `!auth.currentUser` — signed-in cold start no longer performs a local-only overwrite that could race the auth-merge write. | MEDIUM | Structural fix verified by reading; the original finding's own acceptance criteria (a real two-device signed-in test) was **not** performed — see `DATA-001` status `PARTIAL` in `state/FINDING_REGISTRY.md`. |
| E2-DATA-02 | E3_STATIC + E1_RUNTIME | `src/services/storage.ts` (`saveUserData` returns `boolean`), `src/state/useUserProgress.ts` (`noteSaveOutcome`), `src/app/AppNavigator.tsx` (toast wiring) | Two consecutive save failures now surface a one-shot toast via the existing `useToast()` mechanism. Verified by code inspection; the actual failure path itself was not forced/reproduced on-device this session (would require mocking AsyncStorage at the native layer). | MEDIUM | Normal-path (non-failure) behavior was confirmed unaffected via a full on-device walkthrough; the failure-triggering path itself is inspection-only. |
| E2-DEPLOY-01 | E1_RUNTIME | `gh run list --limit 15`, `gh run view <id>` | Every CI run on this repository, from the earliest visible history (Sprint 6, `2026-08-24T19:02`) through this session's own pushes, failed identically with "The job was not started because an Actions budget is preventing further use." CI never actually executed successfully at any point evidenced. | HIGH | Only runs visible via `gh run list --limit 15` were inspected; older history was not exhaustively paged through, but the pattern across 15 consecutive runs across ~10 different sprints/days is a strong signal, not a single data point. |
| E2-DEPLOY-02 | E1_RUNTIME | `.github/workflows/ci.yml` deletion + `git status`/`gh run list` post-push check | Confirmed no new Actions run was triggered by the push that removed the workflow file — CI usage is now genuinely zero, not merely unconfigured. | HIGH | — |
| E2-ARCH-01 | E3_STATIC | `grep -rn "services/spacedRepetition" .` (repo-wide, including `tests/`) — corrected search from baseline's `src/`-only scope | `src/services/spacedRepetition.ts` is a live 1-line re-export (`export * from "../domain/review/spacedRepetition"`) used by `tests/testSuite.ts` — **not** dead code, contradicting the original `ARCH-001` finding's broader claim. `src/i18n/formatters.ts` remains genuinely dead (zero importers repo-wide) and was deleted. | HIGH | This is a correction of RUN-001-BASELINE evidence, disclosed in `09_FINDINGS.md` and `FIX-2026-08-25-03/README.md`. |
| E2-ARCH-02 | E3_STATIC | `src/domain/practice/answer.ts:4`, `src/state/useUserProgress.ts:11` | Both layering-inversion imports corrected: `answer.ts` now imports `evaluateBadges`/`updateDailyQuests` from `../gamification/badges` directly; `useUserProgress.ts` now imports `AnswerQualityMeta` from `../domain/review/qualitySignal` instead of a `features/` hook. | HIGH | — |
| E2-ARCH-03 | E3_STATIC | `wc -l src/screens/AuthScreen.tsx src/features/profile/components/AccountManagementCard.tsx` | 662→337 lines and 594→292 lines respectively, extracted into 6 new single-responsibility files (styles, reusable field/panel components). Re-rendered on-device and screenshot-compared against pre-refactor captures — visually identical. | HIGH | — |
| E2-SEC-01 | E3_STATIC | `.env.example`, direct read | All 6 values are now clearly-fake placeholders (`your-project-id`, etc.), no real Firebase identifiers remain. | HIGH | — |
| E2-DEP-01 | E4_TOOL | `npm audit --json`, `npm ls re2 @opentelemetry/core uuid` | 17 moderate / 0 high / 0 critical across 1214 scanned dependencies. Every affected package traced to `firebase-tools` (dev-only CLI) or `expo-sharing → @expo/config-plugins → xcode` (native-project-generation, build-time only) — confirmed via direct dependency-tree tracing, not assumed. | HIGH | `npm audit fix --force`'s breaking `firebase-tools` downgrade was deliberately not applied — see finding `DEP-001` (`ACCEPTED_RISK`). |
| E2-ACC-01 | E1_RUNTIME | `adb shell settings put secure enabled_accessibility_services ...` + `uiautomator dump` (5 screens/modals) | Real TalkBack service enabled; accessibility tree inspected directly. Found and fixed: hint button malformed label (`", İpucu"` → `"İpucu"`), audio button with zero accessible name (→ `"Telaffuzu dinle"`). Verified via before/after tree dumps. | HIGH | Onboarding flow and Level Promotion celebration modal not reachable/tested this pass (state-dependent) — disclosed in `docs/roadmap/09-accessibility.md`. |
| E2-ACC-02 | E1_RUNTIME | `adb shell settings put system font_scale 2.0` + `ffmpeg` cropped screenshot comparison | Found and fixed: mascot speech bubble (`GardenHeroCard.tsx`) broke the word "başlayalım" mid-character at max system font scale. First fix attempt (`minimumFontScale`) verified ineffective via rebuild+redump; second fix (`maxFontSizeMultiplier={1.3}`) verified effective via a third rebuild+redump. Level Switcher modal's `enabled`/`selected` accessibility states independently verified correct (not a false-positive). | HIGH | Only 3 screens tested at max scale (home, practice, exit-confirm dialog); Profile/Progress/Auth not covered this pass. |
| E2-BUILD-01 | E1_RUNTIME | `gradlew assembleRelease` (final pass, this reaudit) | BUILD SUCCESSFUL; installed on Pixel_9_Pro emulator; launched; `adb logcat` clean (only the pre-existing benign HWUI format warning). | HIGH | Single-emulator smoke pass, as throughout this whole audit — not a device matrix. |

## Global evidence limitations (unchanged from baseline)
No production crash-reporting/telemetry service; Maestro E2E and Firestore rules tests not executed live this reaudit pass (no code touched their surface since baseline, and the CI job that would have run them was removed before ever executing); iOS path remains unbuildable from this checkout.
