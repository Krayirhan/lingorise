# CODE-QUALITY-001-BASELINE — Hotspot Matrix

Revision: `16b9aab1f776503ec52067d4f543af8ab6f2e9aa`

| Symbol / File | Why hot | Actual risk (verified) | Status |
|---|---|---|---|
| `src/services/firestore.ts` (`syncUserData`, `syncUserProgress`, `mergeAndSyncUserData`) | High fan-in (called from `useUserProgress.ts`, `AppBootstrap.tsx`); three independent field declarations for the same underlying concept | Real, demonstrated — the compiler does not enforce that `syncUserProgress`'s manually-curated field subset stays in sync with `UserData`'s shape; already caused real drift (a field silently omitted, scored as DATA-QA-002 in DATA-001-BASELINE; architectural root cause scored as ARCH-QA-001 in ARCHITECTURE-001-BASELINE) | CODE-QA-003, RELATED ROOT CAUSE: ARCH-QA-001 — lightly weighted here |
| `src/domain/practice/answer.ts` (`applyPracticeAnswer`) | Core reward-calculation function, high fan-in via `useUserProgress.ts` | Inspected directly: branching (first-encounter / due-review / neither) is inherent to the domain, well-commented, pure, appropriately sized (~55 lines). The one real issue is the unused `_sessionMode` parameter (RELATED ROOT CAUSE: CORE-QA-001, CORE-001-BASELINE) — an API-signature-clarity concern, not a complexity concern | CODE-QA-004 (API clarity only) — no complexity finding |
| `src/state/useUserProgress.ts` (463 lines) | Largest state hook, high fan-in (indirectly consumed by nearly every screen via `AppNavigator`) | Inspected: the many setters are cohesive facets of one concept (user progress/settings), not a grab-bag of unrelated responsibilities. No internal God-function found — each setter is a short, single-purpose `updateAndPersist` call | PASS — large but cohesive, not a finding |
| `src/services/storage.ts` (391 lines, migration pipeline) | Versioned schema migration logic, safety-critical (every app launch depends on it) | Inspected: well-isolated, self-detecting/idempotent migration steps (`migrateV1ToV2`, `migrateV2ToV3`), thoroughly commented rationale for non-obvious design choices (shape-detection over version-number trust) | PASS — a genuine strength, not a hotspot risk |
| `src/domain/learning/mastery.ts` (`summarizeMastery`, `countMasteredWords`) | Two fully-implemented, test-exercised (7+ call sites in `tests/testSuite.ts`) domain functions | Verified via full-repo reference search: **zero production call sites**. Production's `calculateGardenProgress` is fed `rewardedQuestionIds.length` / a separately-computed `learnedWordsGlobal` value instead — neither of these functions' output is used anywhere in `src/` outside their own definitions | CODE-QA-002 (dead code) |
| `src/services/contentService.ts` (`getNextPracticeQuestion`) | Sole export of the file | Verified via full-repo reference search: zero call sites anywhere | CODE-QA-002 (dead code) |
| `src/services/storage.ts` (`importUserDataJSON`) | Counterpart to the actively-used `exportUserDataJSON` | Verified via full-repo reference search: zero call sites — the export path is wired into `DataManagementCard.tsx`'s UI, but the import counterpart is implemented and never invoked from anywhere | CODE-QA-002 (dead code) |
| `src/services/errorReporter.ts` (`reportError`) | Designed as a future Sentry/Crashlytics hook (per its own comment) | Verified via full-repo reference search: zero call sites — an unwired placeholder | CODE-QA-002 (dead code) |
| `src/screens/PracticeHubScreen.tsx` (480), `GardenHeroCard.tsx` (476), `WordNotebookModal.tsx` (420), `DataManagementCard.tsx` (448) | Largest files by line count | Spot-checked at a high level: primarily UI/presentation composition, not deeply nested business logic. No demonstrated complexity or duplication defect found in these files at the depth checked this pass — large but not automatically a finding per this audit's own scope rule | NOT FULLY VERIFIED at full depth — no finding without concrete evidence (UNKNOWN ≠ FAIL) |

## Summary

The codebase's genuine hotspots are concentrated in exactly the two categories this audit found concrete evidence for: (1) the Firestore sync layer's three independently-maintained field declarations (implementation manifestation of a separately-scored architecture root cause), and (2) a small, bounded set of dead exported functions. No God-modules, no demonstrated deep-nesting/complexity defects, and no harmful duplication were found in the domain or state layers actually inspected.
