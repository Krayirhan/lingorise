# SPRINT-001 — Finding Closure Matrix

| Finding ID | Original domain | Original severity | Root cause | Fix | Executable evidence | Reviewer verdict | Recommended status |
|---|---|---|---|---|---|---|---|
| DATA-QA-001 | Data Integrity | P1 | Remote fetch failure treated identically to remote-absent | `RemoteUserDataResult`/`decideMergeAction` distinguish `found`/`absent`/`failed`; `failed` throws `RemoteStateUnknownError`, never authorizes a push/overwrite | YES — `tests/testSuite.ts` §56, 3 `decideMergeAction` assertions (absent→first-sync, failed→unknown-remote-state, found→merge) | code-reviewer: confirmed distinction genuine and enforced; test-reviewer: confirmed post-fix coverage closes the initial overclaim | **CLOSED** |
| DATA-QA-002 | Data Integrity | P1 | No canonical progress-field ownership; 8 fields silently discarded on merge | `mergeUserData()` + `PROGRESS_FIELD_STRATEGY` registry classify and correctly merge every field, including the 8 historically-omitted ones | YES — `tests/testSuite.ts` §56, named regression test reproducing the exact historical scenario, plus per-field assertions for all 8 fields | Both reviewers: confirmed real, correctly evidenced, root cause (not just symptom) addressed after the day-boundary correction | **CLOSED** |
| DATA-QA-003 | Data Integrity | P2 | "Irreversible" reset claim false for signed-in users; reset additionally pushed wiped state to the cloud via `refresh()` | `reloadLocalOnly()` replaces `refresh()` in the reset flow (never syncs to Firestore); confirm copy now differs for cloud-synced vs. guest accounts | E2 (direct code read + full typecheck/suite pass) — no component-level test possible in this test architecture (pre-existing limitation, not newly introduced) | code-reviewer: confirmed `reloadLocalOnly` genuinely never touches Firestore | **CLOSED** |
| DATA-QA-004 | Data Integrity | P3 | Non-atomic Firestore-then-Auth deletion ordering; incomplete cleanup scope | `dailyTasks` now purged in `deleteUserData()`; `PartialAccountDeletionError` makes the partial-failure state explicit instead of a generic error | E2 (direct code read) — `deleteUserData`/`deleteAccount` require live Firebase Auth/Firestore to exercise end-to-end | code-reviewer: confirmed `deleteUserData` idempotent, retry-safe, no false atomicity claim made | **CLOSED** (see `08_RESIDUAL_RISK.md` for the honestly-documented client-architecture limitation) |
| MAINT-QA-001 | Maintainability | P2 | No canonical schema for "user progress" | `PROGRESS_FIELD_STRATEGY: Record<keyof UserData, FieldStrategy>` — compiler-enforced; a new `UserData` field without a registry entry fails to typecheck | YES — `npm run typecheck` PASS; `tests/testSuite.ts` §56's registry-completeness backstop assertion | code-reviewer: confirmed root cause addressed for the merge layer; noted (correctly) that `normalizeUserData`'s separate manual field list is NOT covered by this same protection | **CLOSED for the merge layer** (its scored root cause). The `normalizeUserData` storage-layer analog is a distinct, smaller, newly-identified gap — see `08_RESIDUAL_RISK.md`, owned by a future targeted pass, not blocking this closure |
| VERIFY-QA-001 | Verification Assurance | P1 | Merge test hand-reimplemented production logic | `tests/testSuite.ts` §56 now calls `mergeUserData()`/`decideMergeAction()` directly; no hand-copied algorithm remains | YES — confirmed by grep and direct reading by both reviewers | Both reviewers: confirmed no duplicated algorithm anywhere in the diff | **CLOSED** |
| VERIFY-QA-002 | Verification Assurance | P3 | Tautological clock-anomaly test (`assert(true, ...)`) | `detectClockAnomaly()` extracted as a pure function; §53 rewritten with 10 real assertions against its actual output | YES — `tests/testSuite.ts` §53 | test-reviewer: confirmed genuinely real, boundary case included, not tautological | **CLOSED** |
| SEC-QA-003 | Security & Privacy | P3 | Non-atomic delete ordering; `dailyTasks` never purged (same root cause as DATA-QA-004) | Same fix as DATA-QA-004 | Same as DATA-QA-004 | Same as DATA-QA-004 | **CLOSED** (see `08_RESIDUAL_RISK.md`) |
| RELEASE-QA-001 | Release Readiness | P1 (proven blocker) | Direct consequence of DATA-QA-002 | Same fix as DATA-QA-002 | Meets the Release Blocker Closure Standard: (1) historical scenario no longer reproduces the loss — YES, named regression test; (2) failed/transient sync + restart is safe — YES, explicit test; (3) damaged state not re-persisted — YES, `mergeUserData` never discards a union/monotonic field; (4) real production merge logic exercised by tests — YES, confirmed by both reviewers; (5) `passedLevelExams` survives relevant scenarios — YES, dedicated + A→B→A + stale-snapshot tests; (6) related history/state fields handled intentionally — YES, per-field classification with day-boundary correction; (7) existing XP/streak/learningProgress/solved/rewarded/badge behavior does not regress — YES, explicitly asserted unchanged | Both reviewers: AGREE the underlying defect is fixed and evidenced; code-reviewer's HIGH day-boundary finding was applied before this closure recommendation was finalized | **CLOSED** |

## Related Architecture/Code Quality evidence (historical files NOT modified)

| Finding | Status |
|---|---|
| ARCH-QA-001 (`ARCHITECTURE-001-BASELINE`) | RELATED EVIDENCE RESOLVED — its scored root cause (no canonical progress schema) is now addressed by `PROGRESS_FIELD_STRATEGY` for the merge layer. The historical audit file itself is unmodified. |
| CODE-QA-003 (`CODE-QUALITY-001-BASELINE`) | RELATED EVIDENCE RESOLVED — the compiler-silent field-list drift it described in `syncUserProgress()`'s manual field subset is a separate, narrower, still-open observation (that function intentionally writes only a curated subset to the lightweight `progress/main` doc, not the full canonical set) — noted in `08_RESIDUAL_RISK.md`, not silently closed. |

Both historical Architecture/Code Quality baseline files remain unmodified, per the immutability requirement.
