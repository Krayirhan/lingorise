# 06 — Evidence Index (REAUDIT)

New/updated evidence since `RUN-002-REAUDIT/06_EVIDENCE_INDEX.md` (that index's entries remain valid for anything not re-touched this session).

| Evidence ID | Level | Check/source | Result | Confidence | Limitations |
|---|---|---|---|---|---|
| E3-DEPLOY-01 | E1_RUNTIME | `gh run list --limit 8` | Latest run on current `HEAD` (`29ce04e`): `32900631213`, all jobs succeeded, triggered by the push that added `29ce04e` itself. Prior run `32899393092` (commit `1b616e0`) also green. | HIGH | Only last 8 runs inspected; sufficient to confirm current green state, not a full history audit (not needed — prior FIX-09 README already documents the full failure history). |
| E3-DEPLOY-02 | E1_RUNTIME | `gh run view 32900631213` | Job breakdown: `verify` 50s ✓, `android-build` 9m32s ✓, `e2e-smoke` 10m17s ✓. Artifact `e2e-smoke-debug` present (retained per-run debug capture, not failure-only). | HIGH | — |
| E3-DEPLOY-03 | E3_STATIC | `npm run test:rules` (local) | Fails locally: `firebase-tools no longer supports Java version before 21` (local Java is 1.8.0_401). Cross-checked `.github/workflows/ci.yml` — `verify` job installs Temurin JDK 21 specifically for this step, and that job passed in CI (E3-DEPLOY-02). | MEDIUM-HIGH | Not independently re-executed against a live Firestore emulator on this machine this pass; relying on CI's equivalent, current, passing run of the identical script as the authoritative signal — disclosed as `NOT EXECUTED (local)` rather than guessed. |
| E3-SEC-01 | E3_STATIC | `gh api repos/Krayirhan/lingorise` | `"private": false`, `"visibility": "public"`. `security_and_analysis`: `secret_scanning: disabled`, `secret_scanning_push_protection: disabled`, `dependabot_security_updates: disabled`. | HIGH | — |
| E3-SEC-02 | E3_STATIC | `git ls-files \| grep -iE '\.(pem\|p12\|jks\|keystore)$\|service-account\|firebase-adminsdk'` | Only match: `android/app/debug.keystore` — pre-existing, already-known (see `RUN-001-BASELINE`), the app's actual release-signing key by design (not a leaked production secret; `android/app/build.gradle` itself documents this needs replacing before a real Play Store submission). No new secret files found. | HIGH | Pattern-based scan, not a full entropy/regex secret scanner; consistent with the depth used at baseline for the same check. |
| E3-SEC-03 | E1_RUNTIME | `gh variable list` | 6 `EXPO_PUBLIC_FIREBASE_*` repository variables, values are Firebase Web SDK client identifiers (API key, project ID, sender ID, etc.) — by Firebase's own design these are not secrets (client-embedded, security enforced by `firestore.rules`, matching this project's own already-closed `SEC-002` reasoning). Repository variables are visible only to accounts with at least write access to the repo, not to anonymous public visitors of a public repo. | HIGH | — |
| E3-SEC-04 | E3_STATIC | `firestore.rules` | Not re-read this pass — no code change touched it since `RUN-002-REAUDIT` (confirmed via `git diff --stat 3436a1b..29ce04e`, no `firestore.rules` entry). Prior verification (rules-enforced tenant isolation, `RUN-001-BASELINE`) remains valid. | HIGH | Reused evidence, not re-verified this pass — appropriate per `14_REAUDIT_PROTOCOL.md`'s "avoid unnecessary re-reading if unchanged." |
| E3-REL-01 | E3_STATIC | `src/services/catalogueService.ts:9-19` (`REMOTE_FETCH_TIMEOUT_MS = 8000`, `Promise.race`) | Direct read confirms the 8-second timeout wraps both `getDoc`/`getDocs` calls, engaging the pre-existing cache → bundled-content fallback chain on timeout instead of hanging indefinitely. | HIGH | Not independently re-tested against a real throttled-network condition this pass (relies on FIX-09's own local verification — rebuild/reinstall, ~6s normal-case render time — plus CI's `e2e-smoke` now passing reliably as indirect confirmation the fallback doesn't break the common path). |
| E3-REL-02 | E3_STATIC | `src/app/AppBootstrap.tsx:26-45` (`authTimeout`, 8000ms) | Direct read confirms an 8-second fallback sets `authUser` to `null` (guest) if `onAuthStateChanged` hasn't resolved, clearing the timer if the real callback fires first. | HIGH | Same as above — relies on FIX-09's local verification plus CI's `e2e-smoke` pass as indirect confirmation; not independently re-tested against a real degraded-auth-network condition this pass. |
| E3-CI-01 | E1_RUNTIME | `.github/workflows/ci.yml`, full read | 3 jobs (`verify`, `android-build`, `e2e-smoke`), each installing correct JDK version (21 for `firebase-tools`/rules test, 17 for Gradle), `android-build`/`e2e-smoke` writing `.env` from repository variables before building, `e2e-smoke` capturing screenshot/uiautomator/logcat/Maestro-hierarchy debug artifacts unconditionally (`if: always()`) for future diagnosability. | HIGH | — |
| E3-CI-02 | E3_STATIC | `.maestro/smoke.yaml`, full read | Full guest-onboarding flow: launch → wait for wildcarded `.*Başla.*` → level selection → guest-start (`.*Misafir Olarak.*`, deliberately not the earlier ambiguous `.*Hemen Başla.*`) → optional notification-permission dismissal → home tab tap → final assertion. Each selector choice is commented with the specific root-cause evidence that drove it (see `FIX-2026-08-25-09/README.md` Rounds 1-11). | HIGH | — |
| E3-BUILD-01 | E1_RUNTIME | `git status` (this reaudit) | Clean working tree, branch up to date with `origin/main`. | HIGH | — |

## Global evidence limitations (unchanged from RUN-002-REAUDIT)

No production crash-reporting/telemetry service; iOS path remains unbuildable from this checkout; `DATA-001`'s real two-device signed-in test remains unperformed (no second authenticated device/session available in this environment); `ACC-001`'s remaining scope (onboarding, Level Promotion modal, Accessibility Scanner, `reduceMotion` audit) remains untested — neither touched by this reaudit's change surface, so neither re-verified nor re-scored differently.
