# EAS Production Configuration

## Expo project identity

`app.json`: `expo.name` = "LingoRise", `expo.slug` = "lingorise", `expo.scheme` = "lingorise", `expo.version` = "0.1.0". No `expo.extra.eas.projectId` field present in `app.json` — EAS project linkage (if any) would be established at first `eas build`/`eas init`, which requires an authenticated session not available here (confirmed: `npx eas-cli whoami` → "Not logged in").

## Android package

`expo.android.package` = `com.lingorise.app` (`app.json`), consistent with `android/app/build.gradle`'s `applicationId 'com.lingorise.app'` — verified, matches the historically-expected identity, no drift found.

## Version / versionCode

- `app.json` `expo.version` = `0.1.0`
- `android/app/build.gradle`: `versionCode 1`, `versionName "0.1.0"` (the committed native Android project, generated by a prior `expo prebuild`)
- `eas.json`'s `production` profile sets `"autoIncrement": true` — EAS will automatically increment `versionCode` on each production build, so the committed `versionCode 1` is a starting point, not a value that needs manual incrementing before the first RC.

**Decision: no manual version bump made this sprint.** `0.1.0` / auto-incrementing versionCode is appropriate for a first production Play release candidate; per Section 11's instruction, only one intentional versioning decision should ever be made, and none is warranted yet — EAS's own `autoIncrement` already handles this at actual build time, and repeated local edits would just be undone/duplicated by that mechanism.

## Production build profile

`eas.json`:
```json
"production": { "autoIncrement": true, "android": { "buildType": "app-bundle" } }
```
Confirms the correct artifact type — **AAB (Android App Bundle)**, not APK — for the production profile, consistent with the intended EAS → AAB → Google Play path. The `preview` profile (internal APK) is correctly a separate, non-production profile.

## Required environment variables (names only — no values inspected or exposed)

`EXPO_PUBLIC_FIREBASE_API_KEY`, `EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN`, `EXPO_PUBLIC_FIREBASE_PROJECT_ID`, `EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET`, `EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`, `EXPO_PUBLIC_FIREBASE_APP_ID` — confirmed by direct read of `src/services/firebase.ts` (all 6 are read via `process.env.EXPO_PUBLIC_FIREBASE_*`); no other `EXPO_PUBLIC_*` variable is referenced anywhere else in `src/`.

## Channel / environment

No `eas.json` `channel` field is set on the `production` profile (EAS defaults channel-per-profile behavior when omitted); no separate "environment" (EAS Environments feature) reference found in `eas.json`. This is a minimal, valid, if unremarkable, EAS config — not a defect, just unconfigured beyond what's needed for a single production channel.

## Build command

`package.json`: `"build:android:production": "eas build --platform android --profile production"` — present and correctly targets the production profile.

## Source provenance requirement

Per Section 15's rule and `06_RELEASE_DEPENDENCY_MAP.md`'s own release-prep gate ("working tree clean/reviewed at the intended release revision"), an EAS build's provenance should map to one exact, ideally committed, source revision. The current working tree contains all of Sprint 1 + Sprint 2 + this sprint's uncommitted changes on top of HEAD `16b9aab...`. No production build was attempted from this dirty state (see `05_SIGNING_AND_ARTIFACT_PROVENANCE.md`, `13_RESIDUAL_EXTERNAL_CONDITIONS.md`).

## Was an actual production build run this sprint?

**NO.** `npx eas-cli whoami` returned "Not logged in" — no authenticated EAS session exists in this environment, and an EAS account login is an account-credential action outside what this sprint can or should perform on the user's behalf. Even if a session existed, Section 14/15's own rule would still require a source-revision lock (a clean, intentional commit) before treating any resulting AAB as the release candidate — which itself requires user commit authorization not given this sprint. Both conditions independently prevent a production build here.

**Status: BLOCKED EXTERNALLY on two independent conditions** — (1) no authenticated EAS session, (2) no committed/locked source revision (by design, per the absolute commit/push rule). See `13_RESIDUAL_EXTERNAL_CONDITIONS.md`.
